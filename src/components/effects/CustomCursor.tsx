import React from 'react';

// CustomCursor has been disabled in favor of standard native browser cursor to prevent dual-cursor bug.
export const CustomCursor: React.FC<{ enabled?: boolean; act?: number }> = () => {
  return null;
};
