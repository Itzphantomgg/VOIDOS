# VOID//OS

> **"The system adapts to observe. The observer adapts to the system."**

![VOID//OS](https://img.shields.io/badge/System-VOID%2F%2FOS_v4.09.2a-00f0ff?style=for-the-badge&logo=gnubash&logoColor=black)
![Architecture](https://img.shields.io/badge/Stack-React_19_+_TypeScript_+_Vite_+_Tailwind-ff007f?style=for-the-badge)
![Audio Engine](https://img.shields.io/badge/Audio-Procedural_Web_Audio_API-b24bf3?style=for-the-badge)

**VOID//OS** is an immersive browser-based interactive game and fake operating system combining late-1990s / early-2000s retro computing, glitchcore visual aesthetics, ARG puzzle mechanics, and an unfolding digital horror narrative.

The operating system itself **is** the game.

```
=====================================================
VOID//OS OPERATING SYSTEM - WORKSTATION REVISION 4.09
AETHELGARD COGNITIVE RESEARCH LABORATORY
=====================================================
```

---

## 🖥️ Core Features

- **Retro Multi-Window Environment**: Movable, 8-directional resizable, minimizable, maximizable, focusable windows with realistic z-index layering.
- **Desktop & Taskbar**: Complete desktop with draggable marquee selection, Start launcher with program search, active window tabs, and system tray (volume flyout, network status, anomaly radar, and clock).
- **100% Procedural Web Audio API Soundscape**: Synthesized mechanical clicks, keypresses, window swooshes, notification chimes, error beeps, boot chords, horror stings, continuous CRT/HDD hum, and procedural synth loops without external audio asset lag.
- **Visual Shaders & Custom Cursors**: CRT scanlines, curvature vignette, phosphor bloom, RGB chromatic aberration, and pixel cursors.
- **Virtual File System (VFS)**: Complete directory tree (`/System`, `/Users`, `/Documents`, `/Downloads`, `/Pictures`, `/Applications`, `/Logs`, `/Archive`, `/Trash`, `/VOID`), previewers for text, images, hex dumps, and audio logs, plus dynamic story-driven file mutations.
- **Interactive Terminal**: Monospace CLI with command history, tab auto-completion, standard Unix/DOS commands, and secret puzzle directives (`override`, `manifest`, `witness`, `void`).
- **Live Task Manager**: Process monitoring with CPU/RAM graphs and rogue daemons (`observer.exe`, `void.exe`) that resist termination.
- **Intranet Browser**: Search engine (`netseek.internal`), corporate research portal (`aethelgard.lab`), operator BBS (`techbbs.retro`), Dr. Sterling's archived diary (`archive.diary`), and the reactive dark web portal (`voidnet.core`).
- **Messages & Email Clients**: Branching chat dialogues with `USER_07`, `SYSTEM ADMIN`, `Dr. Valerie Sterling`, and `VOID//CORE`, alongside an email client with lore threads.
- **Notes & Media Player**: Autosaving scratchpad with system-injected phantom notes and an audio player with a live canvas spectrum visualizer.
- **4-Act Narrative & 6 Endings**: Progressive story engine with 6 distinct endings (*Escape*, *Corruption*, *Loop*, *The Truth*, *Acceptance*, and *The Secret Master Archivist*).
- **Persistent Save System**: Auto-saves state to `localStorage` with export/hard reset support.

---

## 🚀 Getting Started

### Prerequisites

- Node.js (v18 or newer recommended)
- npm or pnpm / yarn

### Installation

```bash
# Clone the repository
git clone https://github.com/<your-username>/VOIDOS.git

# Navigate into directory
cd VOIDOS

# Install dependencies
npm install

# Start local development server
npm run dev
```

Open your browser at `http://localhost:5173`.

### Production Build

```bash
npm run build
npm run preview
```

---

## 🕹️ System Commands Reference

Inside the **Terminal** application:

| Command | Description |
| :--- | :--- |
| `help` | Display list of available diagnostic commands |
| `ls` / `dir` | List contents of directory |
| `cd <path>` | Change active working directory |
| `cat <file>` | Display text contents of file |
| `whoami` | Inspect active operator identity and clearance |
| `ps` | Display active process table |
| `kill <pid>` | Attempt process termination |
| `scan` | Run hardware and port diagnostics |
| `ping <target>`| Probe network interfaces |
| `status` | Telemetry report & anomaly index |
| `logs` | View recent kernel and security logs |
| `override <KEY>` | Execute master security directives |
| `manifest` | Render neural consciousness matrix |
| `witness` | Amplify system anomaly resonance |
| `void` | Direct communication with VOID |

---

## 📜 License

Created for the VOID//OS Interactive Operating System Experience.
All lore, characters, and simulated systems are fictional.
